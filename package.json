{
  "name": "xls-zmp-timesheet-filler",
  "version": "0.1.0",
  "private": true,
  "description": "Local UI and browser agent for ZMP timesheet filling.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start-job": "node scripts/start-job.js",
    "verify-holiday-api": "node scripts/verify-holiday-api.js",
    "check": "node --check server.js && node --check agent/zmpAgent.js && node --check agent/workCalendar.js && node --check scripts/start-job.js && node --check scripts/verify-holiday-api.js"
  },
  "dependencies": {
    "playwright": "^1.53.0"
  }
}
